#include<ostream>
using namespace std;

int main(){
    //  Find Average of An  Array;;
    int  n, sum =0;
    float average;

    cout<<"Enter Size of an Array : ";cin>>n;

    for(int i=0;i<n;i++){
        cin>>[i];
    }

    for(int j=0;j<n;j++){
        sum = sum + a[j];
    }

        average = sum/n;
        cout<<"The Average of an Array"
         return 0;
}
